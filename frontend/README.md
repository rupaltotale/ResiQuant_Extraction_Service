# Frontend (Next.js)

## Setup

```bash
cd frontend
npm install
npm run dev
```

By default the app runs on http://localhost:3000.

Set `NEXT_PUBLIC_BACKEND_URL` if your backend is not on http://localhost:5000.

